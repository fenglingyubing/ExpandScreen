<Window x:Class="ExpandScreen.UI.Views.UnhandledExceptionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ExpandScreen"
        Height="560"
        Width="860"
        MinHeight="520"
        MinWidth="760"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <LinearGradientBrush x:Key="HeaderBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#3D151B2D" Offset="0"/>
            <GradientStop Color="#2B151B2D" Offset="0.32"/>
            <GradientStop Color="#26FFB300" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="IconPillStyle" TargetType="Border">
            <Setter Property="Background" Value="#22FFB300"/>
            <Setter Property="BorderBrush" Value="#44FFB300"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="999"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>

        <Style x:Key="TitleBarButtonStyle" TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
        </Style>

        <Style x:Key="ChipStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="999"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>

        <Style x:Key="MonoBoxStyle" TargetType="TextBox">
            <Setter Property="FontFamily" Value="{DynamicResource MonoFont}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="#0F000000"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="14"
            Background="{DynamicResource BackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Background="{StaticResource HeaderBrush}" CornerRadius="14,14,0,0">
                <Grid Height="56" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource IconPillStyle}" Margin="14,0,10,0" VerticalAlignment="Center">
                        <TextBlock Text="!"
                                   FontFamily="{DynamicResource MonoFont}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource WarningBrush}"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="发生错误"
                                   FontFamily="{DynamicResource UIFont}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="已记录日志。你可以复制详情用于反馈。"
                                   FontFamily="{DynamicResource UIFont}"
                                   FontSize="12"
                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>

                    <Button Grid.Column="2"
                            Style="{StaticResource TitleBarButtonStyle}"
                            Margin="0,0,12,0"
                            Content="✕"
                            Click="ExitButton_Click"
                            ToolTip="退出"/>
                </Grid>
            </Border>

            <!-- Body -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14">
                    <StackPanel>
                        <TextBlock Text="{Binding Summary}"
                                   FontFamily="{DynamicResource UIFont}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   TextWrapping="Wrap"/>

                        <WrapPanel Margin="0,10,0,0">
                            <Border Style="{StaticResource ChipStyle}">
                                <TextBlock Text="{Binding TimestampText}"
                                           FontFamily="{DynamicResource MonoFont}"
                                           FontSize="12"
                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </Border>
                            <Border Style="{StaticResource ChipStyle}">
                                <TextBlock Text="{Binding VersionText}"
                                           FontFamily="{DynamicResource MonoFont}"
                                           FontSize="12"
                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </Border>
                            <Border Style="{StaticResource ChipStyle}">
                                <TextBlock Text="{Binding LogDirText}"
                                           FontFamily="{DynamicResource MonoFont}"
                                           FontSize="12"
                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </Border>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="1"
                           Margin="2,14,2,8"
                           Text="异常详情"
                           FontFamily="{DynamicResource UIFont}"
                           FontSize="12"
                           Foreground="{DynamicResource TextSecondaryBrush}"/>

                <TextBox Grid.Row="2"
                         Style="{StaticResource MonoBoxStyle}"
                         Text="{Binding Details}"/>

                <Grid Grid.Row="3" Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="1"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Content="复制详情"
                            Click="CopyButton_Click"/>

                    <Button Grid.Column="3"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Content="继续运行"
                            Click="ContinueButton_Click"/>

                    <Button Grid.Column="5"
                            Style="{DynamicResource PrimaryButtonStyle}"
                            Content="退出"
                            Click="ExitButton_Click"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>

