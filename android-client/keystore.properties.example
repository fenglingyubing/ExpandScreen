# Copy to `keystore.properties` (DO NOT commit the real file).
#
# Option A: File-based (recommended for local release builds)
# - storeFile supports relative or absolute paths.
#
# storeFile=expandscreen-release.jks
# storePassword=YOUR_STORE_PASSWORD
# keyAlias=expandscreen
# keyPassword=YOUR_KEY_PASSWORD

